/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Allow images from any domain (useful for future CMS integration)
    remotePatterns: [],
  },
};

module.exports = nextConfig;
